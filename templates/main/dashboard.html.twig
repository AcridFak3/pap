{% extends 'base.html.twig' %}

{% block title %}Ticket Dashboard{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center">
        <div class="container mx-auto p-6 rounded-lg shadow-lg bg-gray-900">
            <h1 class="text-3xl font-bold text-white text-center mb-6">Ticket Management</h1>

            <div class="flex justify-center space-x-4">
                <a href="{{ path('app_ticket_all') }}" class="bg-blue-500 text-white px-6 py-3 rounded-lg hover:bg-blue-600 transition duration-300">
                    View Tickets
                </a>
                <a href="{{ path('app_ticket_new') }}" class="bg-green-500 text-white px-6 py-3 rounded-lg hover:bg-green-800 transition duration-300">
                    Create New Ticket
                </a>
            </div>
        </div>
    </div>
{% endblock %}
